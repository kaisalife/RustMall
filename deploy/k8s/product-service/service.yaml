apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: simple-trade
  labels:
    app: product-service
    app.kubernetes.io/part-of: simple-trade
spec:
  selector:
    app: product-service
  ports:
    - port: 50052
      targetPort: 50052
      name: grpc
  type: ClusterIP
