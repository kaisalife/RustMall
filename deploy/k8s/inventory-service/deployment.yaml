apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: simple-trade
  labels:
    app: inventory-service
    app.kubernetes.io/part-of: simple-trade
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
        - name: inventory-service
          image: ghcr.io/kaisalife/rustmall/inventory-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50054
              name: grpc
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: APP_INVENTORY_SERVICE__HOST
              value: "0.0.0.0"
            - name: APP_INVENTORY_SERVICE__PORT
              value: "50054"
            - name: APP_INVENTORY_SERVICE__ADVERTISE_HOST
              value: "inventory-service"
            - name: APP_DATABASE__MAX_CONNECTIONS
              value: "30"
            - name: APP_NACOS__ENABLED
              value: "true"
          readinessProbe:
            grpc:
              port: 50054
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            grpc:
              port: 50054
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
