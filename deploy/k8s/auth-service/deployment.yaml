apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: simple-trade
  labels:
    app: auth-service
    app.kubernetes.io/part-of: simple-trade
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/kaisalife/rustmall/auth-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50051
              name: grpc
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: APP_AUTH_SERVICE__HOST
              value: "0.0.0.0"
            - name: APP_AUTH_SERVICE__PORT
              value: "50051"
            - name: APP_AUTH_SERVICE__ADVERTISE_HOST
              value: "auth-service"
            - name: APP_DATABASE__MAX_CONNECTIONS
              value: "20"
            - name: APP_NACOS__ENABLED
              value: "true"
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
