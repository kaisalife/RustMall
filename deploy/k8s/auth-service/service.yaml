apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: simple-trade
  labels:
    app: auth-service
    app.kubernetes.io/part-of: simple-trade
spec:
  selector:
    app: auth-service
  ports:
    - port: 50051
      targetPort: 50051
      name: grpc
  type: ClusterIP
