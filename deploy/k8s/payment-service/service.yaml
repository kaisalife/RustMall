apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: simple-trade
  labels:
    app: payment-service
    app.kubernetes.io/part-of: simple-trade
spec:
  selector:
    app: payment-service
  ports:
    - port: 50056
      targetPort: 50056
      name: grpc
  type: ClusterIP
