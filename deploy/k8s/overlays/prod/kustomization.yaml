# Prod 环境 Overlay
# 使用前：替换所有 <REPLACE_ME> 为真实生产地址
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: simple-trade
resources:
  - ../../base
patches:
  - path: external-services-patch.yaml
  - path: config-patch.yaml
  - path: secrets-patch.yaml
  - path: replicas-patch.yaml
