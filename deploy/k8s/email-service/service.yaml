apiVersion: v1
kind: Service
metadata:
  name: email-service
  namespace: simple-trade
  labels:
    app: email-service
    app.kubernetes.io/part-of: simple-trade
spec:
  selector:
    app: email-service
  ports:
    - port: 50055
      targetPort: 50055
      name: grpc
  type: ClusterIP
