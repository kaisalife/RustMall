apiVersion: v1
kind: Service
metadata:
  name: nacos
  namespace: simple-trade
  labels:
    app: nacos
    app.kubernetes.io/part-of: simple-trade
spec:
  selector:
    app: nacos
  ports:
    - port: 8848
      targetPort: 8848
      name: http
    - port: 9848
      targetPort: 9848
      name: grpc
  type: ClusterIP
